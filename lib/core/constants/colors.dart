import 'package:flutter/material.dart';

/// Classe contenant toutes les couleurs utilisées dans l'application TIKA
/// Centralise la palette de couleurs pour une gestion cohérente
class AppColors {
  // Couleurs générales de l'application
  static const Color background = Color(0xFFF5F5F5);
  static const Color text = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color cardBackground = Colors.white;
  static const Color iconBlue = Color(0xFF4169E1);
  static const Color iconYellow = Color(0xFFFFB800);

  // Couleurs principales (violet)
  static const Color primary = Color(0xFF9C27B0); // Violet principal
  static const Color accent = Color(0xFFCE93D8); // Violet clair
  static const Color dark = Color(0xFF4A148C); // Violet foncé

  // Couleurs pour l'écran splash
  static const Color splashBackground = Colors.white;

  // Couleurs pour l'onboarding
  static const Color onboardingPurple = Color(0xFF832093); // Violet principal onboarding
  static const Color onboardingGray = Color(0xFFD1D1D1);
  static const Color onboardingText = Color(0xFF832093); // Texte violet
  static const Color onboardingTextSecondary = Color(0xFF323232); // Texte secondaire

 
  static const Color secondary = Color(0xFF7E57C2); // Violet clair
 
}

